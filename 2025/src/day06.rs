use std::cmp::max;
use std::collections::HashMap;
use std::fs;

pub fn main() {
    let mut silver = 0;
    let mut gold: u64 = 0;
    println!("silver: {}", silver);
    println!("gold: {}", gold);
}
